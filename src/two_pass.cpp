//
// Created by matth on 6/18/2025.
//

#include "two_pass.h"



long int two_pass::calculate(const std::vector<std::string> &&calcTokens) {
    // implicitly lead with a "0", "+"
    int workval = 0;
    MathOpr workopr = MathOpr::PLUS;
    auto endItr = cend(calcTokens);
    return 0;
}
